---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: servicebindingingresses.service-bind-ingress.bailu.io
spec:
  group: service-bind-ingress.bailu.io
  names:
    kind: ServiceBindingIngress
    listKind: ServiceBindingIngressList
    plural: servicebindingingresses
    singular: servicebindingingress
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceBindingIngress is the Schema for the servicebindingingresses
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ServiceBindingIngressSpec defines the desired state of ServiceBindingIngress
            properties:
              enable_ingress:
                description: EnableIngress is a flag to enable ingress,it can be empty
                type: boolean
              enable_service:
                description: EnableService is a flag to enable service,requeired
                type: boolean
              image:
                description: Image is the image to use,requeired
                type: string
              replicas:
                description: Replicas is the number of replicas,requeired
                format: int32
                type: integer
            required:
            - enable_service
            - image
            - replicas
            type: object
          status:
            description: ServiceBindingIngressStatus defines the observed state of
              ServiceBindingIngress
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
